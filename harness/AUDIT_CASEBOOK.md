# AUDIT_CASEBOOK — 크롤·판정 오류 원장 (오답노트)

> **목적**: 감사(블라인드 판독 대조)에서 **확정된** 오류를 축적한다. 단순 수리 기록이 아니라,
> 다음 감사가 "이 오류의 형제가 다른 데도 있지 않을까?"를 묻는 **가설의 원천**이다.
>
> **운영 규칙**
> 1. 기재 자격: 대조관 심문을 통과해 원문 증거로 **확정**된 오류만. (블라인드 판독 불일치만으로는 미기재)
> 2. 매 감사 시작 전, 이 원장을 읽고 **형제 오류 가설**을 만든다 → 그 회차 표본의 절반을 가설 검증에 배정.
> 3. 각 항목은 반드시 5요소: 증상 / 원문 증거 / 깨진 가정 / 클래스 수정 / 회귀 테스트.
> 4. 수정은 케이스가 아니라 **클래스**를 고친다 — 이 원칙이 예외처리 지옥과의 차이다.

## 형식

```
### C-### | YYYY-MM-DD | 트랙(리스팅/권리/시세) | 심각도
- 증상:       (무엇이 틀리게 서빙됐나)
- 원문 증거:   (fixture 경로 또는 원문 인용)
- 깨진 가정:   ("코드는 ___라고 가정했지만 실제로는 ___")
- 클래스 수정: (커밋 해시 + 무엇을 일반화했나)
- 회귀 테스트: (tests/ 파일::테스트명)
- 형제 가설:   (같은 가정이 숨어 있을 만한 다른 위치 — 다음 감사 표적)
```

## 소급 등재 (원장 도입 이전의 확정 오류 — 형제 가설 검증용 시드)

### C-001 | 2026-07-23 | 권리 | CRITICAL
- 증상: 관계미상 전입세대(임차신호 없음)를 소유자로 단정해 무시 → 대항력 위험 미탐 (부산서부 2022타경3289 삼환: 전입 1996-10-14 < 말소기준 2002-04-23인데 clean 서빙)
- 원문 증거: 현황조사서 "소유자와의 관계를 알 수 없는 ○씨 세대가 전입… 임대차 관계 등은 알 수 없었음"
- 깨진 가정: "보증금·확정일자·용도가 없으면 임차인이 아니다(=소유자다)"
- 클래스 수정: possible_opposable(대항력 여지) verdict 도입 + 소유자 '점유 명시'만 제외(_asserts_owner_occupancy) — 미커밋(2026-07-23 오전 작업분)
- 회귀 테스트: tests/test_courtauction_detail.py(여지 6종+리뷰 3종), tests/test_curst_tenant.py, tests/test_web.py(end-to-end)
- 형제 가설: ① "결론성 단어가 없으면 위험이 없다"고 가정하는 다른 판정 경로(유치권·지분·가처분 문구 판정) ② 날짜 비교 없이 라벨만 보는 곳

### C-002 | 2026-07-15→07-22 | 권리 | CRITICAL (2회 재발 패턴)
- 증상: 자유텍스트 필드 실명 PII 마스킹 배선 누락 — maejibun(07-15) 수정 후 형제 필드 senior_lien·appraisal_notes(07-22)에서 동일 누락 재발견
- 원문 증거: docs/crawler_qa_layer1.md C1, versions.md 07-22 19:40
- 깨진 가정: "마스킹은 주요 필드에 걸었으니 됐다" (신규/형제 자유텍스트 필드가 자동 보호된다는 착각)
- 클래스 수정: C1 커밋 7cf2225 + backfill --check(잔여 스캔) + pre-commit 게이트 PII 스캔 편입(2026-07-23)
- 회귀 테스트: 마스킹 회귀 6종 + scripts/precommit_gate.ps1 4단계
- 형제 가설: 새 자유텍스트 컬럼 추가 시 재발 — 게이트 PII 스캔이 상시 방어, 감사는 "스캔이 못 보는 저장소(Supabase Storage·로그·evidence)"를 표적

### C-003 | 2026-07-20 | 리스팅 | CRITICAL
- 증상: 부분수집(소량 응답 샤드)이 전량교체(replace_all)를 타고 정상 데이터를 파괴 — 권리 백로그 연쇄 삭제
- 원문 증거: versions.md 07-20 11:35, QA C3
- 깨진 가정: "HTTP 200이면 그 샤드는 완전한 데이터다"
- 클래스 수정: 커버리지 플로어(E1, c7fdf84) + NATIONWIDE_PARTIAL 강등
- 회귀 테스트: E1 게이트 테스트
- 형제 가설: "200이면 정상" 가정이 남은 곳 — 현황조사서 ipcheck=false(E3로 일부 해소), 네이버 빈 응답, 건축물대장 no_bld(F2 미완: 실패와 '없음' 미구분)

### C-004 | 2026-07-19 | 시세 | HIGH
- 증상: 단지명 유사 매칭으로 엉뚱한 단지가 대조군 혼입 → 시세 튐 (이후 complexNo 기반으로 전환)
- 원문 증거: versions.md 해당 항목 (naver 매칭 개편 기록)
- 깨진 가정: "이름이 비슷하면 같은 단지다"
- 클래스 수정: complexNo 기반 매칭 전환
- 회귀 테스트: (보강 필요 — 시세 트랙 감사에서 확인)
- 형제 가설: "식별자가 같으면 실체가 같다"의 다른 사례 — complexNo 재사용/오배정, 면적형(평형) 동치 가정, 국토부 단지명↔네이버 단지명 브리지. → **시세 가정 명세서**(docs/시세_가정_명세서.md)가 전수 목록

<!-- 새 확정 오류는 위 형식으로 이 아래에 추가 -->
