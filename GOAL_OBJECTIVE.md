# GOAL — 객관화 개편 (세금·사진·점수폐지·수집확대)

> 2026-07-02 grill로 확정된 8개 결정의 구현 계약. **Default-FAIL**: 아래 증거를 Read로 확인하기 전엔
> 어떤 항목도 완료로 표시하지 않는다. 편집 시 versions.md 기입(전역 규칙).

## 확정 결정 (변경 금지 — 변경은 사용자 승인 필요)
1. 예상차익 = 추정시세 − (최저입찰가 + 취득세). 취득 시점 표면차익. 양도·보유세는 숫자 미반영, 참고 안내만.
2. 취득세 = 매수인 프로필 config 1개(기본 1주택·비조정·개인). 전 물건 동일 기준, UI에 가정 명시.
3. 세금 지식문서 = 경매 전과정(취득세 정밀·양도세·보유세·경매특유비용) + 기준연도 + 공식근거 출처.
4. 사진/노후화 = 준공연도(건축물대장) + 카카오 로드뷰 + 감정평가서(D). 키 없이도 코드 완성, 키 도착 시 활성.
5. 정렬 = 예상차익 금액(기본) / 갭률 토글.
6. 긍정 판정 뱃지 폐지. 경고만 유지: 권리미확인 / 위험 / 시세추정불가 / 차익없음.
7. arb_score UI 완전 제거(정렬·표시·필터에서). 내부 계산·DB 컬럼은 보존(백테스트·회귀 비교용).
8. 수집 = 전국 17시도 · 현금 ≤ 5억. 스케줄러 -Cash 5억으로 변경.

## 작업 항목 (순서 = 의존성)

### T1. 세금 지식문서 + 검증 `docs/tax-auction-knowledge.md`
- 내용: (a) 경매 취득 = 유상승계취득(판례) → 일반 취득세 적용 근거 (b) 주택 유상취득 세율표:
  6억↓ 1% / 6~9억 (가액×2/3억−3)% 누진 / 9억↑ 3%, 지방교육세 = 주택세율×1/2×20%,
  농특세 = 전용 85㎡ 초과 0.2% (c) 다주택 중과(조정 2주택 8%·3주택+ 12% / 비조정 3주택 8%·4주택+ 12%,
  중과 시 농특·교육세 상향) (d) 법인 12% (e) 비주택(오피스텔·상가·토지) 4%+0.4%+0.2%=4.6%
  (f) 양도세 개요(단기 중과 1년↓70%/2년↓60%, 1주택 비과세 요건) — 참고용
  (g) 보유세 개요(재산세·종부세) — 참고용 (h) 경매특유비용(명도·미납관리비 공용부분·인지세·채권할인·등기) — 참고용
- **세율 수치는 웹(공식/신뢰 소스)으로 검증 후 기재**, 기준연도(2026)와 출처 URL 명시.
- ✅ 증거: 문서 존재 + 세율표가 코드 상수와 일치(테스트로 고정).

### T2. 취득세 엔진 재작성 `src/tax.py` (신규)
- `BuyerProfile`(주택수, 조정지역 여부, 법인 여부) — config/JSON로 설정(기본 1주택·비조정·개인).
- `acquisition_tax(price, property_type, area_m2, profile)`:
  주택(구간·누진·중과·농특 85㎡·교육세) / 비주택 4.6%. 문서 T1과 1:1 일치.
- score.py는 tax.py 호출로 전환. 기존 단순식 제거.
- ✅ 증거: 경계값 테스트(6억/9억/85㎡/중과 2·3주택/비주택) 전부 통과, 문서 수치와 assert 일치.

### T3. UI 전환 — 점수 제거·차익 중심
- listings: 스코어 열 제거 → 1열=예상차익 금액(크게)+갭률(보조). 정렬 토글 profit(기본)/gap.
  긍정 뱃지 제거, 경고 뱃지만. 히어로=차익 1위 물건(경고 있으면 경고 노출).
- detail: 점수 카드(가격갭/권리/환금 점수) 제거 → 차익 근거(객관)+세금 참고 패널(취득세 상세,
  양도·보유세는 텍스트 안내)+준공연도 자리.
- methodology: 가중치·등급 섹션 → 차익 산식·취득세 가정·경고 기준으로 재작성.
- query.py: SORT_KEYS 기본 profit. min_score 필터 → min_profit(억) 필터로 교체.
- ✅ 증거: pytest(웹 테스트 갱신) 통과, 렌더에 arb_score·긍정뱃지 문자열 부재, 경고 뱃지 존재.

### T4. 준공연도·로드뷰 준비 (키 대기형)
- 건축물대장: building_register_client 준공연도(useAprDay)→ 물건에 `built_year`·경과연수 표시(키 활성 시).
  미활성 시 "준공연도 미확인" 표시.
- 로드뷰: detail에 카카오 로드뷰 임베드 블록(KAKAO_JS_KEY env 있으면 렌더, 없으면 자리+안내).
  좌표는 주소→V-World 지오코더(키 보유) 또는 카카오 주소검색.
- ✅ 증거: 키 없는 상태에서 페이지 정상(자리표시), 키 주입 mock 테스트 통과.

### T5. 수집 확대 + 라이브 반영
- refresh-daily.ps1 / install-scheduler.ps1: -Cash 500000000.
- 전국 1회 실크롤(--nationwide --cash 5억 --live) → replace_all 적재 → 서버 재시작.
- ✅ 증거: auction.db 건수(수백 건 기대), /health db, 목록 렌더.

## 안전 불변식 (감사에서 확정 — 유지)
- 권리미확인 게이트·최소표본 게이트·시세출처 가드·PII sanitize·크롤 일일상한/지터 유지.
- 경고 뱃지는 어떤 경우에도 숨기지 않는다.

## 완료 정의
pytest 전부 green + ruff clean + T1~T5 증거 확인 + versions.md 기입 + commit/push + 서버 최신 반영.
