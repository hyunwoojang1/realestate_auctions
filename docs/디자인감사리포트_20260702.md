# 🎨 디자인 감사 리포트 — auction-arbitrage (2026-07-02 11:48 KST)

3관점 병렬(접근성 WCAG 2.2 · 디자인품질/안티템플릿 · 반응형/CSS) + 색 대비 직접 계산.
"Warm-Paper Financial Broadsheet" 리디자인 대상.

## 종합: 🟡→🟢(수정 후)
preview(정제 샘플)에선 기준 충족했으나 (1) 접근성 대비 다수 실패 (2) 실데이터에서 안전-색 신호 붕괴
(3) 모바일 미대응이 발견됨. 확정 이슈 일괄 수정 완료(pytest 177·ruff 클린·전 토큰 AA 통과).

## 발견 & 조치
### 🔴 CRITICAL — 수정 완료
1. **권리미확인인데 주변이 다 초록** — 뱃지만 중립, score/gap/profit 3채널이 초록으로 '매수' 신호.
   → `.unv` 클래스로 권리미확인 물건의 점수·크기막대·갭·차익을 muted로 강등(리스트 행·히어로·상세). 안전 원칙 시각 일관.
2. **색 대비 AA 실패**(계산): b-top 3.71·muted 4.37·muted-2 2.37·warn 3.58·thead 4.06·score<60 2.37.
   → 토큰 하향: muted #565D68·muted-2 #666C77·warn/conf-mid #A44E09·score-lt60 #787E88·b-top fill=pos-strong(#0A7A3E, 흰글씨 5.43). **전부 통과**.
3. **모바일 미대응**(@media 1개뿐) → 560px 브레이크포인트 추가(필터 세로 전폭·44px 타깃·카드 1열·gm-lab 랩·히어로 조정).

### 🟠 HIGH — 수정 완료
4. 음수갭 gapmeter 붕괴(꽉 찬 막대=손실 오인) + gm-appr 감정가 마커 클리핑/접근불가
   → `report._gap_meter` 재작성: 최저가≥시세면 적색 초과막대(gm-over)+"차익없음"(gm-loss), gm-appr 제거(감정가는 상세 kv).
5. 실데이터 등급열 회색 벽 → `.unv` 강등 + score 크기막대로 서브랭크 시각화(부분 완화). *잔여: 상위 소집단 밴드/접기(후속).*
6. `차익없음`↔`시세추정불가` 동일 토큰 → `b-na`(점선, 정보부족) 분리. `b-none`(실선, 계산됨).
7. 인사이트 밴드 항상 초록 → `차익 유력 0건`이면 `.insight.flat`(muted 좌측바).
8. 빈 단지명 → `apt_name or property_type` 폴백(빈 링크 제거).

### 🟡/⚪ — 수정 완료
color-mix 정적 폴백(gm-min/site/conf)·th scope=col·주소 ellipsis/line-clamp·conf "실거래 N건"·한글 라벨 uppercase 제거(자간 축소)·죽은 CSS(.sc-90~/.f-90/.hero-why .q) 제거.

### 잔여(후속, 저우선)
- 실데이터 위계: 권리미확인 상위 N 밴드 승격 + 시세추정불가 기본 접기.
- 히어로↔표 랭크 연속성 표기.
- gm-appr(감정가 눈금) 접근가능 형태로 재도입 여부.
- 필터 버튼 solid blue vs "블루=링크 전용" 톤(선택).
- progressbar role/aria(현재 인접 텍스트로 값 제공되어 정보손실 낮음).

## 잘된 점(유지)
gapmeter(뱃지+델타 페어)·방법론 정직성(샘플/합성 명시)·웜페이퍼 토큰·tabular/slashed-zero mono ID·hairline 테이블.
