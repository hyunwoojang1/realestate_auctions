# 🔎 감사 리포트 — auction-arbitrage (2026-07-02 11:07 KST · deep)

7관점 병렬 리뷰어(architect·security·python·silent-failure·performance·database·domain-money) +
실구동 검증. 교차검증(★=복수 관점 동의) 후 종합.

## 종합 판정: 🔴→🟢(수정 후) — "코드·정직성 설계는 탄탄하나 라이브는 위험했다"
빌드·테스트는 완벽(pytest 169→173, ruff 클린)하나, **권리분석 계층 미배선**으로 모든 실매물이
'권리 깨끗·공실'로 채점돼 '확실한 차익' 뱃지가 권리불명 물건에 붙을 수 있었음. + **공개레포 실데이터 노출**.
→ 이번 세션에서 CRITICAL 전부 + 주요 HIGH 수정 완료.

## 1) 실제 구동 (사실)
| 항목 | 결과 |
|---|---|
| pytest | ✅ 169(감사 전) → **173**(수정 후, 신규 4) |
| ruff | ✅ 클린 |
| 정적 렌더 | ✅ 6페이지 200 OK |
| repo | 🔴→🟢 PUBLIC → **PRIVATE** + 히스토리 정리 |

## 2) 발견 & 조치 (★=교차검증)

### 🔴 CRITICAL — 전부 수정
| # | 발견 | 관점 | 조치 |
|---|---|---|---|
| 1 | ★★★★ 권리 미배선 → 실매물 rights_score≈100, 게이트 무력, 상세 "안전✓" 허위 | arch·silent·domain(+테스트가 assert) | `rights_verified` 게이트: 미검증=`권리미확인` 등급·경고문, '차익 유력' 금지 |
| 2 | ★★★ 표본 1건으로 시세 확정·점수 부여 | arch·silent·domain | 실거래 <2=시세추정불가, '차익 유력'은 ≥3건 |
| 3 | ★★ 샘플/픽스처 시세가 서빙 DB에 'db'로 노출 | arch | run.py 시세출처 가드(비-라이브=*.dryrun.db) |
| 4 | ★ 공개레포 실경매 데이터(사건번호+주소) | security | repo private + untrack + ignore강화 + 히스토리 제거 |
| 5 | ★ PK=case_no뿐 → 다물건 사건 덮어씀 | database | (다음) doc_id/물건번호 PK — 문서화 |

### 🟠 HIGH
| 발견 | 관점 | 조치 |
|---|---|---|
| ★★★ SQLite WAL 없음 → 새로고침 중 락→샘플 폴백 | arch·db·perf | WAL+busy_timeout |
| ★★ 팔린/취하 매물 미삭제 | arch·db | store.replace_all(전국 풀스냅샷 교체) |
| ★★ 단지명 부분일치 동명이단지 오매칭 | arch·domain·py | matcher 법정동 제약+최소명길이 |
| ★★ /methodology 허구 백테스트를 '신뢰근거'로 | arch·silent | 샘플/합성 기준 캐비엇 상단 노출 |
| ★ 점유 기본 '공실'→명도비0 | domain | 기본값 '소유자점유' |
| ★ .dockerignore 실데이터 미제외 | security | data 로컬json·evidence 제외 |
| ★ 매요청 DB재연결·전체스캔 | perf | (다음) 커넥션 캐시 — 문서화 |

### 🟡 MEDIUM / ⚪ LOW — 처리
config 검증(경고)·라이브 부분실패 집계·`_redact` 공개·VWORLD .env.example·to_html 이스케이프·area0 로그 ✅ /
미납관리비 원가·to_won 명명·acq_tax 경계테스트 = (다음).

## 3) 잘 된 점 (유지)
시세추정불가 정직경로 · 크롤러 안전태세(403중단·킬스위치·지터·부분결과) · X-Data-Source 헤더 ·
PII sanitize · 파라미터라이즈드 SQL · 하드게이트 설계 · ScoreConfig 외부화.

## 4) 이번 감사가 보장 / 미검증
- ✅ 정적 7관점 교차검증 + 실구동(테스트·린트·렌더).
- ❌ 라이브 크롤 실호출(샌드박스), 실제 낙찰결과 기반 정확도(백테스트는 합성).

## 5) 남은 작업(우선순위)
1. E(단독/상업/토지) matcher/score 배선, D(권리파서) 물건상세 파이프라인 배선(→ 배선되면 권리검증 게이트가 실작동).
2. PK doc_id(물건번호) 다물건 분리.
3. 미납관리비 원가 라인, 서버 커넥션 캐시.
4. 스케줄러 첫 발화(익일 05:30) 결과 확인.
