# 밤샘 자동 실행 런북 — 10억 크롤 완료 후 검수·감사·수정 (2026-07-20 야간)

> 이 세션(부동산경매) 전용 자동 작업 명세. 사용자 취침(2026-07-20 ~23:10). 크롤(R2-4) 완료가 트리거.
> **다른 세션이 권리(rights) 크롤을 별도로 진행하므로 이 런북은 권리를 절대 건드리지 않는다.**

## 트리거
- **주 신호**: 백그라운드 체인 태스크(b4p872yfu) 종료 알림 **또는** 지속 모니터(bop3jzohy)의
  `CHAIN_DONE`(chain_live.log에 `=== R2 체인 완료 exit=0`). 둘 중 먼저 오는 것.
- 예상 발화: 2026-07-20 자정~새벽 1시경(사용자 "대충 3시간 뒤" ≈ 02:00 이내).
- `CHAIN_ABORT`(체인 중단)면: 원인 로그 확인 → 네트워크/절전이면 체인 재개, 코드버그면 수정 후 재개.

## 절대 금지(역할 분담)
- ❌ **권리 크롤링** — 다른 세션 담당. crawl_rights·listing_rights·courtauction_detail 관련 실행 금지.
- ❌ **git push / deploy_prod.sh** — 배포는 "다른 작업 다 끝난 뒤" 사용자 조율. 트리에 타 세션
  미커밋분 섞여 있어 지금 배포 위험. 코드 수정은 **로컬 커밋까지만**(push 안 함) 또는 미커밋 유지.
- ❌ 타 세션 파일(web.py·casesearch·naver_* 대개편분·docx) 스테이징/수정.

## 실행 순서 (R2-4 완료 직후)

### 1. DB 스냅샷 백업 (최우선·타이밍 중요)
다른 세션 권리 크롤이 auction.db에 replace_all 쓰기 시작하기 전에 8시간 수집분 보존 +
검증용 안정 스냅샷 확보.
- `sqlite3 backup API`로 `auction.db` → `data/backup/auction_postcrawl_20260720.db`
- 이후 모든 검증은 **이 스냅샷 대상**(라이브 DB 잠금·경합 회피).

### 2. 표본검수 (②) — 신규 5~10억 매칭 품질
스냅샷에서 30~50건 표본(진천 밖, 5~10억 구간 우선):
- est(추정시세) vs KB밴드 정합(이탈 건수)
- market_scope 분포(same_complex_same_area 비율)
- 감정가 괴리·밴드폭 과대 플래그 건수
- 오매칭 의심(N단지/N차·동명이단지) 표본 눈검
- 방식: 병렬 검증 워크플로(읽기 전용).

### 3. 5~10억 성과 리포트
확장 목적 달성도:
- 5~10억 구간 물건 수 / 그중 est 성공 / 같은단지 확정 비율
- 어제(≤5억) 대비 델타
- 그 구간 상위 차익 매물 TOP 20(권리 경고 병기)

### 4. 크롤 데이터 감사 (다관점)
크롤 산출물의 데이터 정합성 감사(코드 아닌 **데이터** 초점):
- 취소거래 재혼입 0 확인, 중복행, 층0 비대칭, 브리지 보충분 정합
- naver_prices↔naver_real_trades 조인 무결성
- 방식: 다관점 워크플로 + 적대검증(확정 결함만 채택).

### 5. 문제 발견 시 코드 수정 (로컬 한정)
- 감사에서 **확정(CONFIRMED) 결함**만 수정. 추정/스타일은 기록만.
- 수정 후 `pytest -q` 그린 확인.
- **push·deploy 금지** — 로컬 변경으로 두고 아침 리뷰 대기.
- 수정 내역 versions.md 기입(KST 시각).

### 6. 결과 MD 리포트
- `경매-크롤-트래커\POSTCRAWL_REPORT_20260720.md`에 1~5 결과 종합:
  성과 수치·검수 발견·감사 확정결함·적용 수정·잔여 사용자 액션.
- 아침에 사용자가 이거 하나만 읽으면 밤새 일이 파악되게.

## 아침 핸드오프(사용자 액션)
- ⑥ UI(VWorld·건축물대장) 배포 여부 — "배포" 한 마디.
- 코드 push·deploy — 다른 세션 권리 작업까지 끝난 뒤 조율.
- ⑧ Vercel GitHub App 레포 허용(자동배포 업그레이드).
- 감사에서 사용자 판단 필요한 MEDIUM/LOW 항목(리포트에 별도 표기).
