# 부동산 경매 차익 큐레이션 (realestate_auctions)

> **경매 최저입찰가가 실거래 시세보다 싸서 "차익이 확실한" 물건만 자동으로 골라주는 엔진**의 PoC.
> 한 줄로: *"검색하는 도구가 아니라, 사라고 콕 집어주는 엔진."*

법원경매 물건의 **최저입찰가**와 **국토부 실거래 추정시세**를 매칭하고, 부대비용(취득세·명도비·수리비·인수금액)을 차감한 **순차익**과 **차익 스코어(0~100)**를 계산해, 차익률 높은 순으로 큐레이션합니다.

---

## 왜 만들었나 — 시장의 빈칸

국내 사설 경매정보 사이트(지지옥션·스피드옥션·탱크옥션·옥션원·태인 등)와 신흥 AI 서비스(요이땅·땅집고·미스고 등) 20여 곳을 분석한 결과, 공통된 구조적 공백이 있었습니다.

> **거의 모든 사이트가 "안전한가(권리분석)"에는 과잉투자했지만, "돈이 되는가(차익)"는 비워뒀다.**

- 레거시 사이트는 시세를 "참고시세/인근 매각사례" 수준으로만 보여주고, **"최저입찰가 vs 실거래 시세 = 예상차익"을 자동 계산해 랭킹·정렬·알림으로 밀어주지 않습니다.**
- "반값경매" 같은 테마 큐레이션도 대부분 **감정가 대비 할인율**일 뿐, **실거래 시세 대비 차익**이 아닙니다. (감정가는 시세가 아닙니다 — 이 착시가 핵심입니다.)

이 프로젝트는 그 빈칸 — **실거래 시세 대비 차익을 1급 정렬키로 삼는 큐레이션** — 을 채웁니다.

### 이 PoC가 검증하는 3가지
1. **시세 대비 싼(차익 큰) 물건을 자동으로 상위에 올린다.**
2. **차익이 커 보여도 권리가 더러우면(인수금액 과다·유치권) "위험"으로 강등한다** (하드게이트).
3. **시세 추정이 안 되는 물건(실거래 매칭 0건)은 거짓 차익을 만들지 않고 "시세추정불가"로 분리한다.**

---

## 어떻게 작동하나

```
[경매 물건]                 [국토부 실거래]
 최저입찰가·감정가·권리      아파트/연립다세대/오피스텔
      │                          │
      └──────────┬───────────────┘
                 ▼
         ┌──────────────┐
         │   매칭/시세추정  │  단지명+전용면적 ±10% → 법정동 폴백
         │  (matcher)    │  평단가 중앙값 × 전용면적 = 추정시세
         └──────┬───────┘
                ▼
         ┌──────────────┐
         │  차익 스코어 엔진 │  가격갭50 + 권리30 + 환금20, ×신뢰계수
         │   (score)     │  + 하드게이트(인수>30%·유치권 → 위험)
         └──────┬───────┘
                ▼
      SQLite 저장 → 콘솔 랭킹표 / CSV / HTML 리포트
```

---

## 차익 스코어 (0~100)

```
score = ( 가격갭 × 0.50 + 권리 × 0.30 + 환금성 × 0.20 ) × 신뢰계수(0.6~1.0)

실질취득원가 = 최저입찰가 + 취득세 + 명도비 + 수리비 + 인수금액
가격갭       = (추정시세 − 실질취득원가) / 추정시세
```

| 하위점수 | 가중치 | 무엇을 보나 | 데이터 소스 |
|---|---|---|---|
| **가격갭** | 50% | 부대비용 차감 후 시세 대비 할인율 | 법원경매 + 국토부 실거래 + 세율 룰 |
| **권리 깨끗함** | 30% | 인수금액 비율·특수권리·대항력 임차인·명도난이도 | 등기·매각물건명세서·현황조사서 |
| **환금성** | 20% | 물건종류·지역유동성·거래회전 | 실거래량 + 자체 낙찰결과 DB |
| **신뢰계수** | ×0.6~1.0 | 시세 추정 표본 밀도 | 실거래 매칭 건수 (3↑=1.0 / 2=0.85 / 1=0.7 / 0=시세추정불가) |

### 하드게이트 (제품의 핵심 안전장치)
**인수금액/최저가 > 30%** 또는 **치명적 유치권**이 있으면 → 권리 점수를 0으로 만들고, **최종 스코어를 상한(25)으로 끌어내려 "위험" 등급으로 강등**합니다.

> 권리 점수만 0으로 만드는 것으로는 부족합니다. 가격갭 가중치가 50%라, 갭이 큰 유치권 물건이 여전히 "양호"로 상위 노출될 수 있기 때문입니다. 그래서 최종 점수 자체를 끌어내립니다. (이 허점은 개발 중 자동 발견·수정되었습니다.)

### 등급
`확실한 차익`(80+) · `양호`(60~79) · `관심`(40~59) · `주의`(<40) · `위험`(하드게이트) · `차익없음`(순차익≤0) · `시세추정불가`(매칭 0건)

---

## 설치

요구사항: Python 3.11+ (개발·검증은 3.14)

```bash
git clone https://github.com/hyunwoojang1/realestate_auctions.git
cd realestate_auctions

python -m venv .venv
# Windows
.venv\Scripts\python -m pip install -r requirements.txt
# macOS / Linux
# source .venv/bin/activate && pip install -r requirements.txt
```

## 실행

```bash
# 샘플 데이터로 차익 큐레이션 (API 키 불필요)
python run.py

# 국토부 라이브 실거래로 검증 (MOLIT_API_KEY 필요)
python run.py --live --ym 202605
```

출력:
- **콘솔**: 차익 스코어 랭킹표
- **`evidence/result.csv`**: 전 컬럼 데이터
- **`evidence/result.html`**: 갭미터·스코어 뱃지 시각화 리포트 (잉크블루+시그널그린)

### 출력 예시 (샘플 데이터)

```
순위  스코어  등급        단지              유형     최저가   추정시세  예상차익   갭    신뢰
1     95   확실한 차익   상계주공            아파트   3.97억  6.34억   2.24억   35%  1.00
2     81   확실한 차익   강남역삼푸르지오시티   오피스텔  2.24억  3.00억   0.71억   24%  1.00
3     52   관심        반석마을아이파크       아파트   3.44억  4.50억   0.89억   20%  0.70
4     50   차익없음     광교호반베르디움       아파트   5.50억  5.60억  -0.02억   -0%  1.00
5     25   위험        해운대마린시티자이      아파트   5.76억  9.55억   1.61억   17%  0.85  ← 인수금액 2억 게이트
6     25   위험        화곡동 다세대         다세대   1.47억  2.80억   1.13억   40%  1.00  ← 유치권 게이트
```

> 4번(광교)은 신건이라 갭이 없어 "차익없음", 5·6번은 표면 차익이 커 보여도 권리 리스크로 "위험" 강등 — **제품의 핵심 동작이 그대로 보입니다.**

---

## 웹 서버 (사이트)

브라우저에서 차익 매물을 열람·필터하는 Flask 사이트.

```bash
# 로컬 실행
python -m flask --app src.web run        # http://127.0.0.1:5000

# Docker
docker build -t auction-arb .
docker run -p 8000:8000 auction-arb      # http://localhost:8000
```

| 경로 | 설명 |
|---|---|
| `/` | 차익 큐레이션 목록 (스코어순, 갭미터·스코어뱃지) + 필터 폼(스코어/종류/지역/정렬) |
| `/property/<사건번호>` | 물건 상세 — 갭미터 특대·차익 근거·권리 안전성(하드게이트 사유)·환금성 |
| `/api/listings?min_score=80&type=아파트&region=서울&sort=profit` | JSON API (필터 쿼리) |
| `/api/listings/<사건번호>` | 단건 JSON |
| `/health` | 헬스체크 |

> 데이터는 PoC 샘플. 국토부 키(`.env`의 `MOLIT_API_KEY`)를 넣으면 라이브 시세로 전환됩니다(F10).

---

## 데이터 소스 (라이브, F10)

| 데이터 | 소스 | 비용 | 비고 |
|---|---|---|---|
| 아파트/연립다세대/오피스텔 실거래가 | 국토교통부 실거래가 API (data.go.kr) | **무료** | 운영자 키 발급 필요 |
| 경매 물건 | 대법원 법원경매정보 | — | PoC는 샘플 fixture, v1에서 크롤러 |

### 국토부 API 키 발급 (라이브 실행용)
1. [공공데이터포털](https://www.data.go.kr) 회원가입
2. "국토교통부_아파트 매매 실거래가 자료" 오픈API → **활용신청** (무료·대부분 자동승인)
3. 발급 인증키를 `.env`에 입력 (`.env.example` 복사):
   ```
   MOLIT_API_KEY=발급받은_키
   ```
4. `python run.py --live --ym 202605`

> ⚠️ 유료 경매사이트의 가공 데이터를 크롤링하지 않습니다. 데이터는 **원천(대법원·국토부 등)에서만** 수집합니다.

---

## 프로젝트 구조

```
realestate_auctions/
├── run.py                  # CLI 엔트리
├── src/
│   ├── models.py           # 도메인 모델 (AuctionListing / Trade / ScoredListing)
│   ├── score.py            # 차익 스코어 엔진 (공식·하드게이트·부대비용)
│   ├── molit_client.py     # 국토부 실거래 API 클라이언트 + XML 파서 (아파트/빌라/오피스텔)
│   ├── matcher.py          # 경매 ↔ 실거래 매칭, 추정시세
│   ├── store.py            # SQLite 저장/조회
│   ├── pipeline.py         # end-to-end 오케스트레이션
│   └── report.py           # 콘솔/CSV/HTML 출력
├── data/                   # 샘플 fixture (경매 6건, 실거래 XML 3종)
├── tests/                  # 단위테스트 (pytest, 17건)
├── GOAL.md                 # 목표·완료기준
├── PROGRESS.md             # 현재 진행 상태
└── versions.md             # 변경 이력 (KST 타임스탬프)
```

## 테스트

```bash
python -m pytest -q     # 17 passed
```
- 차익 스코어 공식·하드게이트, XML 파서(한글/영문 태그), 매칭·시세추정, 음수 차익 처리 등 검증.

---

## 개발 로드맵

- [x] **PoC** — 차익 스코어 엔진 + 아파트/빌라/오피스텔 시세 매칭 + 샘플 end-to-end + HTML 리포트
- [ ] **F10 라이브** — 국토부 API 키 연동, 라이브 실거래로 차익 검증 *(운영자 키 대기)*
- [ ] **v1** — 실제 법원경매(courtauction.go.kr) 크롤러, 온비드 공매 통합, 차익 임계치 알림
- [ ] **v2** — 등기/명세서 자동 파싱 권리분석 고도화, 사후 적중률 대시보드, 웹 UI

> 이 PoC는 **자율 빌드 루프**(GOAL/PROGRESS/versions.md + 증거 기반 게이트)로 단계적으로 구축되었습니다. 진행 이력은 `versions.md` 참고.

## 면책

본 프로젝트의 차익·권리 분석 결과는 **투자 판단을 돕는 참고 정보이며, 정확성을 보증하지 않고 전문가(법무사 등) 상담을 대체하지 않습니다.** 경매 투자에 따른 책임은 이용자 본인에게 있습니다.

## 라이선스

미정 (개인 프로젝트). 사용 전 문의 바랍니다.
